// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"kiwi-user/internal/infrastructure/repository/ent/stripeevent"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// StripeEvent is the model entity for the StripeEvent schema.
type StripeEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// EventID holds the value of the "event_id" field.
	EventID string `json:"event_id,omitempty"`
	// EventType holds the value of the "event_type" field.
	EventType string `json:"event_type,omitempty"`
	// Processed holds the value of the "processed" field.
	Processed bool `json:"processed,omitempty"`
	// ProcessedAt holds the value of the "processed_at" field.
	ProcessedAt  time.Time `json:"processed_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StripeEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case stripeevent.FieldProcessed:
			values[i] = new(sql.NullBool)
		case stripeevent.FieldID:
			values[i] = new(sql.NullInt64)
		case stripeevent.FieldEventID, stripeevent.FieldEventType:
			values[i] = new(sql.NullString)
		case stripeevent.FieldCreatedAt, stripeevent.FieldProcessedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StripeEvent fields.
func (se *StripeEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case stripeevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			se.ID = int(value.Int64)
		case stripeevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				se.CreatedAt = value.Time
			}
		case stripeevent.FieldEventID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_id", values[i])
			} else if value.Valid {
				se.EventID = value.String
			}
		case stripeevent.FieldEventType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_type", values[i])
			} else if value.Valid {
				se.EventType = value.String
			}
		case stripeevent.FieldProcessed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field processed", values[i])
			} else if value.Valid {
				se.Processed = value.Bool
			}
		case stripeevent.FieldProcessedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field processed_at", values[i])
			} else if value.Valid {
				se.ProcessedAt = value.Time
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StripeEvent.
// This includes values selected through modifiers, order, etc.
func (se *StripeEvent) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// Update returns a builder for updating this StripeEvent.
// Note that you need to call StripeEvent.Unwrap() before calling this method if this StripeEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *StripeEvent) Update() *StripeEventUpdateOne {
	return NewStripeEventClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the StripeEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *StripeEvent) Unwrap() *StripeEvent {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: StripeEvent is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *StripeEvent) String() string {
	var builder strings.Builder
	builder.WriteString("StripeEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("created_at=")
	builder.WriteString(se.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("event_id=")
	builder.WriteString(se.EventID)
	builder.WriteString(", ")
	builder.WriteString("event_type=")
	builder.WriteString(se.EventType)
	builder.WriteString(", ")
	builder.WriteString("processed=")
	builder.WriteString(fmt.Sprintf("%v", se.Processed))
	builder.WriteString(", ")
	builder.WriteString("processed_at=")
	builder.WriteString(se.ProcessedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// StripeEvents is a parsable slice of StripeEvent.
type StripeEvents []*StripeEvent
