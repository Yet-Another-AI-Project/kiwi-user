// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"kiwi-user/internal/infrastructure/repository/ent/mailvertifycode"
	"kiwi-user/internal/infrastructure/repository/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// MailVertifyCodeQuery is the builder for querying MailVertifyCode entities.
type MailVertifyCodeQuery struct {
	config
	ctx        *QueryContext
	order      []mailvertifycode.OrderOption
	inters     []Interceptor
	predicates []predicate.MailVertifyCode
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MailVertifyCodeQuery builder.
func (mvcq *MailVertifyCodeQuery) Where(ps ...predicate.MailVertifyCode) *MailVertifyCodeQuery {
	mvcq.predicates = append(mvcq.predicates, ps...)
	return mvcq
}

// Limit the number of records to be returned by this query.
func (mvcq *MailVertifyCodeQuery) Limit(limit int) *MailVertifyCodeQuery {
	mvcq.ctx.Limit = &limit
	return mvcq
}

// Offset to start from.
func (mvcq *MailVertifyCodeQuery) Offset(offset int) *MailVertifyCodeQuery {
	mvcq.ctx.Offset = &offset
	return mvcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mvcq *MailVertifyCodeQuery) Unique(unique bool) *MailVertifyCodeQuery {
	mvcq.ctx.Unique = &unique
	return mvcq
}

// Order specifies how the records should be ordered.
func (mvcq *MailVertifyCodeQuery) Order(o ...mailvertifycode.OrderOption) *MailVertifyCodeQuery {
	mvcq.order = append(mvcq.order, o...)
	return mvcq
}

// First returns the first MailVertifyCode entity from the query.
// Returns a *NotFoundError when no MailVertifyCode was found.
func (mvcq *MailVertifyCodeQuery) First(ctx context.Context) (*MailVertifyCode, error) {
	nodes, err := mvcq.Limit(1).All(setContextOp(ctx, mvcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mailvertifycode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) FirstX(ctx context.Context) *MailVertifyCode {
	node, err := mvcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MailVertifyCode ID from the query.
// Returns a *NotFoundError when no MailVertifyCode ID was found.
func (mvcq *MailVertifyCodeQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mvcq.Limit(1).IDs(setContextOp(ctx, mvcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mailvertifycode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := mvcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MailVertifyCode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MailVertifyCode entity is found.
// Returns a *NotFoundError when no MailVertifyCode entities are found.
func (mvcq *MailVertifyCodeQuery) Only(ctx context.Context) (*MailVertifyCode, error) {
	nodes, err := mvcq.Limit(2).All(setContextOp(ctx, mvcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mailvertifycode.Label}
	default:
		return nil, &NotSingularError{mailvertifycode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) OnlyX(ctx context.Context) *MailVertifyCode {
	node, err := mvcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MailVertifyCode ID in the query.
// Returns a *NotSingularError when more than one MailVertifyCode ID is found.
// Returns a *NotFoundError when no entities are found.
func (mvcq *MailVertifyCodeQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mvcq.Limit(2).IDs(setContextOp(ctx, mvcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mailvertifycode.Label}
	default:
		err = &NotSingularError{mailvertifycode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := mvcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MailVertifyCodes.
func (mvcq *MailVertifyCodeQuery) All(ctx context.Context) ([]*MailVertifyCode, error) {
	ctx = setContextOp(ctx, mvcq.ctx, ent.OpQueryAll)
	if err := mvcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MailVertifyCode, *MailVertifyCodeQuery]()
	return withInterceptors[[]*MailVertifyCode](ctx, mvcq, qr, mvcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) AllX(ctx context.Context) []*MailVertifyCode {
	nodes, err := mvcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MailVertifyCode IDs.
func (mvcq *MailVertifyCodeQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if mvcq.ctx.Unique == nil && mvcq.path != nil {
		mvcq.Unique(true)
	}
	ctx = setContextOp(ctx, mvcq.ctx, ent.OpQueryIDs)
	if err = mvcq.Select(mailvertifycode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := mvcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mvcq *MailVertifyCodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mvcq.ctx, ent.OpQueryCount)
	if err := mvcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mvcq, querierCount[*MailVertifyCodeQuery](), mvcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) CountX(ctx context.Context) int {
	count, err := mvcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mvcq *MailVertifyCodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mvcq.ctx, ent.OpQueryExist)
	switch _, err := mvcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mvcq *MailVertifyCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := mvcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MailVertifyCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mvcq *MailVertifyCodeQuery) Clone() *MailVertifyCodeQuery {
	if mvcq == nil {
		return nil
	}
	return &MailVertifyCodeQuery{
		config:     mvcq.config,
		ctx:        mvcq.ctx.Clone(),
		order:      append([]mailvertifycode.OrderOption{}, mvcq.order...),
		inters:     append([]Interceptor{}, mvcq.inters...),
		predicates: append([]predicate.MailVertifyCode{}, mvcq.predicates...),
		// clone intermediate query.
		sql:  mvcq.sql.Clone(),
		path: mvcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MailVertifyCode.Query().
//		GroupBy(mailvertifycode.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mvcq *MailVertifyCodeQuery) GroupBy(field string, fields ...string) *MailVertifyCodeGroupBy {
	mvcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MailVertifyCodeGroupBy{build: mvcq}
	grbuild.flds = &mvcq.ctx.Fields
	grbuild.label = mailvertifycode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.MailVertifyCode.Query().
//		Select(mailvertifycode.FieldCreatedAt).
//		Scan(ctx, &v)
func (mvcq *MailVertifyCodeQuery) Select(fields ...string) *MailVertifyCodeSelect {
	mvcq.ctx.Fields = append(mvcq.ctx.Fields, fields...)
	sbuild := &MailVertifyCodeSelect{MailVertifyCodeQuery: mvcq}
	sbuild.label = mailvertifycode.Label
	sbuild.flds, sbuild.scan = &mvcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MailVertifyCodeSelect configured with the given aggregations.
func (mvcq *MailVertifyCodeQuery) Aggregate(fns ...AggregateFunc) *MailVertifyCodeSelect {
	return mvcq.Select().Aggregate(fns...)
}

func (mvcq *MailVertifyCodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mvcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mvcq); err != nil {
				return err
			}
		}
	}
	for _, f := range mvcq.ctx.Fields {
		if !mailvertifycode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mvcq.path != nil {
		prev, err := mvcq.path(ctx)
		if err != nil {
			return err
		}
		mvcq.sql = prev
	}
	return nil
}

func (mvcq *MailVertifyCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MailVertifyCode, error) {
	var (
		nodes = []*MailVertifyCode{}
		_spec = mvcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MailVertifyCode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MailVertifyCode{config: mvcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(mvcq.modifiers) > 0 {
		_spec.Modifiers = mvcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mvcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mvcq *MailVertifyCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mvcq.querySpec()
	if len(mvcq.modifiers) > 0 {
		_spec.Modifiers = mvcq.modifiers
	}
	_spec.Node.Columns = mvcq.ctx.Fields
	if len(mvcq.ctx.Fields) > 0 {
		_spec.Unique = mvcq.ctx.Unique != nil && *mvcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mvcq.driver, _spec)
}

func (mvcq *MailVertifyCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mailvertifycode.Table, mailvertifycode.Columns, sqlgraph.NewFieldSpec(mailvertifycode.FieldID, field.TypeUUID))
	_spec.From = mvcq.sql
	if unique := mvcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mvcq.path != nil {
		_spec.Unique = true
	}
	if fields := mvcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mailvertifycode.FieldID)
		for i := range fields {
			if fields[i] != mailvertifycode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mvcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mvcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mvcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mvcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mvcq *MailVertifyCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mvcq.driver.Dialect())
	t1 := builder.Table(mailvertifycode.Table)
	columns := mvcq.ctx.Fields
	if len(columns) == 0 {
		columns = mailvertifycode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mvcq.sql != nil {
		selector = mvcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mvcq.ctx.Unique != nil && *mvcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mvcq.modifiers {
		m(selector)
	}
	for _, p := range mvcq.predicates {
		p(selector)
	}
	for _, p := range mvcq.order {
		p(selector)
	}
	if offset := mvcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mvcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (mvcq *MailVertifyCodeQuery) ForUpdate(opts ...sql.LockOption) *MailVertifyCodeQuery {
	if mvcq.driver.Dialect() == dialect.Postgres {
		mvcq.Unique(false)
	}
	mvcq.modifiers = append(mvcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return mvcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (mvcq *MailVertifyCodeQuery) ForShare(opts ...sql.LockOption) *MailVertifyCodeQuery {
	if mvcq.driver.Dialect() == dialect.Postgres {
		mvcq.Unique(false)
	}
	mvcq.modifiers = append(mvcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return mvcq
}

// MailVertifyCodeGroupBy is the group-by builder for MailVertifyCode entities.
type MailVertifyCodeGroupBy struct {
	selector
	build *MailVertifyCodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mvcgb *MailVertifyCodeGroupBy) Aggregate(fns ...AggregateFunc) *MailVertifyCodeGroupBy {
	mvcgb.fns = append(mvcgb.fns, fns...)
	return mvcgb
}

// Scan applies the selector query and scans the result into the given value.
func (mvcgb *MailVertifyCodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mvcgb.build.ctx, ent.OpQueryGroupBy)
	if err := mvcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MailVertifyCodeQuery, *MailVertifyCodeGroupBy](ctx, mvcgb.build, mvcgb, mvcgb.build.inters, v)
}

func (mvcgb *MailVertifyCodeGroupBy) sqlScan(ctx context.Context, root *MailVertifyCodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mvcgb.fns))
	for _, fn := range mvcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mvcgb.flds)+len(mvcgb.fns))
		for _, f := range *mvcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mvcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mvcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MailVertifyCodeSelect is the builder for selecting fields of MailVertifyCode entities.
type MailVertifyCodeSelect struct {
	*MailVertifyCodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mvcs *MailVertifyCodeSelect) Aggregate(fns ...AggregateFunc) *MailVertifyCodeSelect {
	mvcs.fns = append(mvcs.fns, fns...)
	return mvcs
}

// Scan applies the selector query and scans the result into the given value.
func (mvcs *MailVertifyCodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mvcs.ctx, ent.OpQuerySelect)
	if err := mvcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MailVertifyCodeQuery, *MailVertifyCodeSelect](ctx, mvcs.MailVertifyCodeQuery, mvcs, mvcs.inters, v)
}

func (mvcs *MailVertifyCodeSelect) sqlScan(ctx context.Context, root *MailVertifyCodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mvcs.fns))
	for _, fn := range mvcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mvcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mvcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
